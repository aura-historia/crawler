# DynamoDB Configuration
DYNAMODB_TABLE_NAME=aura-historia-data
DYNAMODB_ENDPOINT_URL=http://localhost:8000

AWS_REGION=eu-central-1
AWS_ACCESS_KEY_ID=fakeMyKeyId
AWS_SECRET_ACCESS_KEY=fakeSecretAccessKey

# Application Settings
LOG_LEVEL=INFO

# Note: These credentials are only for local development with DynamoDB Local.
# For production, use proper AWS credentials with appropriate permissions.